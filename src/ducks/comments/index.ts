import comments from './commentsSlice';

export {selectAttachedComments, selectAllComments} from './selectors';
export {getAllComments, addComment, deleteComment} from './commentsSlice';
export default comments;
